# Codex tool hooks

Diagnose why Codex `PreToolUse` and `PostToolUse` events do not reach the dashboard after hook installation.

## Acceptance criteria

- Identify whether the failure is in Codex hook discovery/execution or dashboard event ingestion.
- Preserve working Claude hooks and existing user configuration.
