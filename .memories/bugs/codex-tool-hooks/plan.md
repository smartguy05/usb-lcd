# Plan

- Verify the installed Codex hook configuration against current official documentation.
- Compare Codex lifecycle logs with dashboard receipt logs.
- Exercise the configured Windows command independently to isolate the boundary.
- Confirm behavior from a newly started Codex session before changing runtime code.
