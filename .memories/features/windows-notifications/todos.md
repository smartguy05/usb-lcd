# Todos

- [x] Rebuild the Windows installer with machine-level AppX certificate trust.
- [ ] Run the complete elevated installer interactively to confirm registration
  on a normal Windows desktop (the build shell cannot accept a UAC prompt).
- [ ] Confirm the repaired installer restores the user's currently incomplete
  installation and paints the physical LCD.
