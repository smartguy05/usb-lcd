# Plan

- Add SQLite-backed todo model/store and immutable render snapshot.
- Add todo widget, daemon context, admin API/UI, and MCP stdio tools.
- Merge/unmerge user-scoped MCP configuration during install/uninstall.
- Add coverage, update docs/version, rebuild and verify both installers.
