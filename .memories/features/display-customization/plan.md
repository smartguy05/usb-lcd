# Plan

- Extend configuration and add shared orientation/layout transforms.
- Map logical frames and partial writes into each physical panel orientation.
- Add deterministic screen-saver rendering and daemon activity tracking.
- Add validated managed uploads and wallpaper-aware translucent rendering.
- Cover behavior with tests, update documentation/version, and rebuild installers.
