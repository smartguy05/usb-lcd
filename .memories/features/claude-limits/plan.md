# Plan

- Add persistent Claude limit snapshots and a rate-limited background Fable fetcher.
- Wire a responsive `claude_limits` widget into the compositor and settings editor.
- Lazy-load CLI-only dependencies and raise managed hook timeouts.
- Add regression coverage, documentation, version metadata, and rebuilt installers.
- Refresh all OAuth usage buckets rather than discarding session and weekly data.
